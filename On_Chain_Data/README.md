# On_Chain_Data

This repository is used to:
Save liquidation events from Compound and AAVE into a Postgres Database.
Uses MistTrack API to check if a user address(es) is malicious.
Create an API that will be used to query data from the Database.

Upon cloning the repo. Run

`npm install`

To run any script run

`node src/nameOfScript`
